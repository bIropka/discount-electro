$(document).ready(function () {

    $('.photo-list li').click(function(){
        if(!$(this).hasClass('active')) {
            var curIindex = $('.photo-list li').index($(this));
            switch (curIindex) {
                case:
            }
        }
    });

});
